---
"changestack-action": minor
---

Initial release: `setup` action installing the changestack CLI from release binaries via the runner tool cache, and a root action running the release + version-PR flow with JSON outputs grouped by package strategy.
